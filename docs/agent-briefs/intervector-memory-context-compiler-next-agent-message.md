# Intervector Agent Message: Memory Context Compiler Continuation

**Created:** 2026-07-06
**Audience:** next Codex, Claude, Claude Code, or specialist MCS v2 agent
**Kevin call phrase:** "go to intervector agent message"
**Weight:** 10/10
**Aliases:** `digital-memory-discovery`, `dmd-mem`, `memory-context-compiler`, `mcc-v1`

## Message To Next Agent

Kevin is not asking you to rediscover the concept. Continue it.

The July 6 discovery is this:

Kevin creates meaning by naming and weighting important moments. The system must memorialize those moments by attaching them to semantic recall and graph traversal, then compile the right Mongo, Neo4j, and Chroma pieces into an agent-operating context shape.

In Kevin's words, the compiler compiles the three functions of separate databases into a memory context shape for agents to live in:

- MongoDB: the canonical memory, container, shelf, or book.
- Neo4j: the relationships, edges, roles, verbs, and paths.
- ChromaDB: the semantic meaning and natural-language retrieval surface.

The core shift is from "AI memory as a database" to "AI memory as compiled meaning." Kevin supplies significance. The system supplies recall, relationships, and context assembly.

## What Codex Already Landed

Codex added the first contract and Steve proof slice:

- `packages/shared/src/runtime/memory-context-compiler-schema.ts`
- `server/src/domain/steveContextComparison.ts`
- `server/src/runtime/context/steveRuntimeContextFoundation.ts`
- `docs/agent-briefs/steve-context-knowledge-base-next-agent-brief.md`
- `organization/ACR-0010-steve-context-comparison-contract.md`

The schema version is:

- `memory_context_compiler.schema.v1`

ACR status:

- `ACR-0010` is proposed, not approved.
- No persistence of compiled shapes or comparison reports is authorized yet.

## Graph Questions

The compiler shape now carries graph questions. These are the questions an agent asks when traversing memory:

- what created this memory?
- what does this memory mean?
- what does this memory support?
- what context does this memory require?
- what agent action does this memory guide?
- what should this memory retrieve?
- what does this memory protect or exclude?
- what does this memory hand off to?

## Graph Verbs

The graph verbs are the action language for memory edges:

- `captures`
- `expresses`
- `supports`
- `requires_context`
- `guides`
- `retrieves`
- `grounds`
- `protects`
- `excludes`
- `hands_off_to`
- `relates_to`
- `supersedes`
- `contradicts`

Use these verbs to describe how a memory ingredient relates to another ingredient or knowledge record.

Example shape:

- Steve discovery `captures` learning style.
- Learning style `expresses` meaning.
- Learning style `supports` approved knowledge.
- Steve handoff `hands_off_to` Michael training/support context.

## Steve Is The First Proof Surface

Do not start with a new memory app before checking Steve.

Steve already creates the app's highest-value first human-context artifact:

- primary why;
- success vision;
- learning style;
- communication preferences;
- support needs;
- launch recommendations;
- training recommendations;
- Michael handoff;
- transcript and discovery answers.

The next implementation should use Steve to prove the compiler can compare a saved Steve interview against approved Knowledge Base context and produce a useful compiled memory context shape.

## Next Build Slice

Continue in this order:

1. Read `docs/agent-briefs/steve-context-knowledge-base-next-agent-brief.md`.
2. Read `organization/ACR-0010-steve-context-comparison-contract.md`.
3. Read `packages/shared/src/runtime/memory-context-compiler-schema.ts`.
4. Read `server/src/domain/steveContextComparison.ts`.
5. Verify current tests still pass.
6. Find a real saved Steve interview artifact, or seed a controlled local fixture if no saved artifact exists in this environment.
7. Run `compareSteveInterviewToContext(...)` against the artifact and a real or fixture Context Packet.
8. Add a small trace/report surface only if it stays non-persistent and does not leak internal packet language to the BA.
9. Do not persist compiled memory reports until Kevin approves ACR-0010 or a follow-up persistence ACR.

## Runtime Boundaries

Do not:

- let Steve query MongoDB, Neo4j, ChromaDB, GraphRAG, or gateway tools directly;
- make Steve score, rank, qualify, classify, or predict a BA;
- expose internal Context Packet ids, retrieval audit, graph traversal, or compiler language to the BA;
- touch `.com` prospect pages for this work;
- introduce income, placement, or automated prospecting language;
- replace the existing Steve discovery flow.

Do:

- let the server assemble Context Packets;
- keep Steve's Context Manager bridge flag-gated;
- keep degraded behavior safe when approved knowledge is missing;
- treat approved briefs as future Knowledge Base source material;
- keep graph verbs as the way memory relationships become actionable for agents.

## Exact Continuation Prompt

Kevin can paste or say this:

> Go to intervector agent message. Continue the July 6 Digital Memory Discovery / Memory Context Compiler work. Read `docs/agent-briefs/intervector-memory-context-compiler-next-agent-message.md`, then the Steve context brief, ACR-0010, the compiler schema, and the Steve comparison code. Do not rediscover the concept. Prove the next slice with Steve by comparing a saved Steve interview to a Context Packet, keep it non-persistent unless ACR-0010 is approved, and preserve the graph questions and verbs as the memory traversal language.

## Meaning To Preserve

This record is itself an example of memorializing essential knowledge.

Kevin identified the significance. Codex turned it into a named, typed, retrievable, testable structure. The next agent should treat this as a weighted memory handle, not a loose note.
