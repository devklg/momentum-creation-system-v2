/**
 * Knowledge Evolution Runtime — events barrel (Lane D).
 *
 * Stable import surface for the in-process event bus, the published/consumed envelopes, and the
 * consumed-trigger helpers. No behavior of its own.
 */

export * from './envelope.js';
export * from './bus.js';
export * from './consumers.js';
